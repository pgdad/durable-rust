---
phase: 15-async-operation-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/closure-style/src/waits.rs
  - examples/macro-style/src/waits.rs
  - examples/trait-style/src/waits.rs
  - examples/builder-style/src/waits.rs
autonomous: true
requirements: [OPTEST-04]

must_haves:
  truths:
    - "All 4 waits.rs handlers accept event-driven wait duration via event[\"wait_seconds\"]"
    - "Updated Lambda images are deployed and reachable via live alias"
  artifacts:
    - path: "examples/closure-style/src/waits.rs"
      provides: "Event-driven wait duration for closure-style"
      contains: "event[\"wait_seconds\"]"
    - path: "examples/macro-style/src/waits.rs"
      provides: "Event-driven wait duration for macro-style"
      contains: "event[\"wait_seconds\"]"
    - path: "examples/trait-style/src/waits.rs"
      provides: "Event-driven wait duration for trait-style"
      contains: "event[\"wait_seconds\"]"
    - path: "examples/builder-style/src/waits.rs"
      provides: "Event-driven wait duration for builder-style"
      contains: "event[\"wait_seconds\"]"
  key_links:
    - from: "examples/*/src/waits.rs"
      to: "ctx.wait()"
      via: "event payload read"
      pattern: "event\\[.wait_seconds.\\]"
---

<objective>
Modify all 4 waits.rs handlers to accept event-driven wait duration, then rebuild Docker images and redeploy Lambda functions so updated binaries are live.

Purpose: Wait tests need a 5-second wait (not 60 seconds) to run in practical time. The handler modification is a prerequisite for all Phase 15 wait tests.
Output: 4 updated waits.rs files, rebuilt Docker images in ECR, updated Lambda aliases pointing to new versions.
</objective>

<execution_context>
@/home/esa/.claude/get-shit-done/workflows/execute-plan.md
@/home/esa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-async-operation-tests/15-CONTEXT.md
@.planning/phases/15-async-operation-tests/15-RESEARCH.md

@examples/closure-style/src/waits.rs
@examples/macro-style/src/waits.rs
@examples/trait-style/src/waits.rs
@examples/builder-style/src/waits.rs

<interfaces>
<!-- Current handler parameter patterns (all use _event which must be renamed to event) -->

closure-style/src/waits.rs:
```rust
async fn handler(
    _event: serde_json::Value,  // rename to event
    mut ctx: ClosureContext,
) -> Result<serde_json::Value, DurableError>
```

macro-style/src/waits.rs:
```rust
async fn handler(
    _event: serde_json::Value,  // rename to event
    mut ctx: DurableContext,
) -> Result<serde_json::Value, DurableError>
```

trait-style/src/waits.rs:
```rust
async fn handle(
    &self,
    _event: serde_json::Value,  // rename to event
    mut ctx: TraitContext,
) -> Result<serde_json::Value, DurableError>
```

builder-style/src/waits.rs:
```rust
|_event: serde_json::Value, mut ctx: BuilderContext| async move {
// rename to: |event: serde_json::Value, mut ctx: BuilderContext| async move {
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Modify 4 waits.rs handlers to read wait duration from event payload</name>
  <files>
    examples/closure-style/src/waits.rs
    examples/macro-style/src/waits.rs
    examples/trait-style/src/waits.rs
    examples/builder-style/src/waits.rs
  </files>
  <action>
In each of the 4 waits.rs files, make these changes:

1. Rename the `_event` parameter to `event` (removing underscore prefix so the compiler binds it)
2. Before the `ctx.wait("cooling_period", 60)` call, add: `let wait_secs = event["wait_seconds"].as_u64().unwrap_or(60);`
3. Change `ctx.wait("cooling_period", 60)` to `ctx.wait("cooling_period", wait_secs)`
4. Update the doc comment at the top to mention event-driven duration

Specific per-file changes:

**closure-style/src/waits.rs** (line 9): `_event` -> `event` in handler signature. Add `let wait_secs = event["wait_seconds"].as_u64().unwrap_or(60);` before line 20 (`ctx.wait`). Change `60` to `wait_secs` on the wait call.

**macro-style/src/waits.rs** (line 12): `_event` -> `event` in handler signature. Same wait_secs extraction before `ctx.wait`. Change `60` to `wait_secs`.

**trait-style/src/waits.rs** (line 12): `_event` -> `event` in handle method signature. Same wait_secs extraction before `ctx.wait`. Change `60` to `wait_secs`.

**builder-style/src/waits.rs** (line 8): `_event` -> `event` in closure parameter. Same wait_secs extraction before `ctx.wait`. Change `60` to `wait_secs`.

After editing, verify compilation: `cargo build --workspace`
  </action>
  <verify>
    <automated>cd /home/esa/git/durable-rust && cargo build --workspace 2>&1 | tail -5</automated>
  </verify>
  <done>All 4 waits.rs files compile successfully with event-driven wait duration. Parameter renamed from _event to event. Default 60s preserved for backward compatibility.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild Docker images and redeploy Lambda functions</name>
  <files></files>
  <action>
Run the existing build and deploy pipeline to push updated waits binaries to AWS:

1. Verify ADFS credentials: `aws sts get-caller-identity --profile adfs --region us-east-2`
2. Build all Docker images: `bash scripts/build-images.sh` — this rebuilds all 48 images (cargo-chef caches dependencies, only recompiles changed waits source). Wait for completion (may take several minutes for the musl cross-compilation).
3. Deploy updated Lambda functions: `terraform -chdir=infra apply -parallelism=5 -auto-approve` — publishes new versions and updates `live` aliases to point to the new images.
4. Verify at least one waits function updated by checking the image URI changed: compare the new version's image URI against the previous version's.

IMPORTANT: Do NOT modify any infrastructure files. This task only runs existing scripts to rebuild and redeploy. If build-images.sh fails, check Docker daemon status (`docker info`) and ECR login (`aws ecr get-login-password --profile adfs --region us-east-2 | docker login ...`).
  </action>
  <verify>
    <automated>cd /home/esa/git/durable-rust && terraform -chdir=infra output -json alias_arns | jq 'keys | map(select(contains("waits"))) | length'</automated>
  </verify>
  <done>All 4 waits Lambda functions (closure-waits, macro-waits, trait-waits, builder-waits) have live aliases pointing to the new published version with event-driven wait duration.</done>
</task>

</tasks>

<verification>
- `cargo build --workspace` succeeds with no errors
- `cargo fmt --all --check` passes (formatting preserved)
- `grep -c 'wait_seconds' examples/*/src/waits.rs` returns 1 for each file (4 total)
- `terraform -chdir=infra output -json alias_arns | jq 'keys | map(select(contains("waits")))' ` lists 4 waits entries
</verification>

<success_criteria>
- All 4 waits.rs files read `wait_seconds` from event payload with default 60
- Docker images rebuilt and pushed to ECR
- Lambda aliases updated to new image versions
- Build + format + clippy checks all pass
</success_criteria>

<output>
After completion, create `.planning/phases/15-async-operation-tests/15-01-SUMMARY.md`
</output>
