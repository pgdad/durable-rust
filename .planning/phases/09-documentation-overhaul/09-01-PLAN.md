---
phase: 09-documentation-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-03
  - DOCS-04
  - DOCS-07

must_haves:
  truths:
    - "README has a 'Determinism Rules' section with do/don't table and code examples after Operations Guide"
    - "README has an error handling section showing Ok(Ok(v)), Ok(Err(e)), Err(durable_err) three-arm match"
    - "README has a Troubleshooting FAQ section covering Send+Static, Serialize bounds, and type annotation errors with actual compiler error text"
    - "README has a 'Contributing / Implementation Rules' section linking to _bmad-output/project-context.md"
    - "README parallel example has an inline comment explaining why boxing/type alias is needed for heterogeneous async closures"
  artifacts:
    - path: "README.md"
      provides: "Complete README with determinism rules, error handling, troubleshooting FAQ, contributing link, and parallel boxing comment"
      contains: "Determinism Rules"
  key_links:
    - from: "README.md"
      to: "_bmad-output/project-context.md"
      via: "markdown link in Contributing section"
      pattern: "project-context\\.md"
---

<objective>
Add five new content sections to README.md: determinism rules, error handling guide, troubleshooting FAQ, contributing link, and parallel boxing comment.

Purpose: Cover the three biggest sources of developer confusion (determinism violations, two-level Result matching, and common compiler errors) directly in the README so developers don't need to discover these patterns through trial and error.

Output: Updated README.md with all five DOCS requirements addressed.
</objective>

<execution_context>
@/home/esa/.claude/get-shit-done/workflows/execute-plan.md
@/home/esa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-documentation-overhaul/09-CONTEXT.md
@.planning/phases/09-documentation-overhaul/09-RESEARCH.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Determinism Rules, Error Handling, and Parallel Boxing Comment to README</name>
  <files>README.md</files>
  <action>
Edit README.md to add three content blocks:

**1. Determinism Rules section (DOCS-01)** — Insert a new `## Determinism Rules` section immediately after the "Replay-Safe Logging" subsection (after line 299, before `## Testing`). Per user decision, this goes after "Operations Guide". Content:
- Opening paragraph: "Code outside durable operations re-executes on every invocation, including replays. Non-deterministic code produces different values each time, breaking replay."
- A do/don't table with three rows: Utc::now(), Uuid::new_v4(), rand::random() — showing wrong (outside step) vs right (inside step) patterns.
- A "Wrong" code block showing Uuid::new_v4() outside a step with comment explaining the problem.
- A "Right" code block showing Uuid::new_v4() inside a step with comment explaining why it works.
- A safety checklist with checkboxes: no Utc::now()/SystemTime::now() outside steps, no Uuid::new_v4() outside steps, no rand::random() outside steps, no env var reads that may differ between invocations outside steps, operation order is fixed.
- Use the exact content from 09-RESEARCH.md DOCS-01 section as the source material.

**2. Error Handling section (DOCS-02)** — Insert a new `## Error Handling` section. Per user decision, place it in "Quick Start" or as a new section. Place it as a new section after "API Styles" and before "Operations Guide" (this is the natural reading flow: learn the APIs, then learn error handling, then learn operations). Content:
- Brief intro explaining the two-level Result: outer `Result<_, DurableError>` for SDK infrastructure errors, inner `Result<T, E>` for business logic.
- Code example showing the `?` unwrap of outer layer, then match on inner layer.
- Full three-arm match pattern: `Ok(Ok(tx_id))`, `Ok(Err(biz_err))`, `Err(durable_err)` with comments.
- Use the exact content from 09-RESEARCH.md DOCS-02 section.

**3. Parallel boxing comment (DOCS-07)** — In the existing "Parallel (concurrent fan-out)" subsection, add a comment block immediately before the `type BranchFn = ...` line (currently line 239). The comment should explain:
- Why type alias and Box::pin are needed.
- "parallel() requires a Vec of type-erased closures because each branch may have a different concrete future type"
- "Box<dyn FnOnce(DurableContext) -> Pin<Box<dyn Future<...> + Send>>> is the standard trait-object pattern for heterogeneous async closures"
- "The BranchFn type alias keeps signatures readable"
- Use the exact content from 09-RESEARCH.md DOCS-07 section.

Important: Do NOT change any existing content. Only INSERT new sections and comments. Preserve all existing section anchors and heading levels.
  </action>
  <verify>
    <automated>cd /home/esa/git/durable-rust && grep -c "Determinism Rules" README.md && grep -c "Ok(Ok(" README.md && grep -c "BranchFn" README.md && grep -c "trait-object pattern" README.md</automated>
  </verify>
  <done>"Determinism Rules" section exists after Operations Guide with do/don't table and code examples. "Error Handling" section exists with three-arm match pattern. Parallel section has inline comment explaining boxing. All three additions are pure insertions with no existing content modified.</done>
</task>

<task type="auto">
  <name>Task 2: Add Troubleshooting FAQ and Contributing Section to README</name>
  <files>README.md</files>
  <action>
Edit README.md to add two content sections:

**1. Troubleshooting FAQ (DOCS-03)** — Insert a new `## Troubleshooting` section near the bottom, before "Container Deployment". Per user decision, this covers the 3 most common compile errors.

Entry 1: **Send + 'static on parallel/map closures**
- Show a representative compiler error (error[E0521]: borrowed data escapes outside of closure).
- Show the fix: clone data before the closure and use `move`.
- Use the content from 09-RESEARCH.md DOCS-03 Entry 1.

Entry 2: **Serialize + DeserializeOwned bounds**
- Show error[E0277]: the trait bound `MyType: Serialize` is not satisfied.
- Show the fix: add `#[derive(serde::Serialize, serde::Deserialize)]`.
- Use the content from 09-RESEARCH.md DOCS-03 Entry 2.

Entry 3: **Missing type annotations**
- Show error[E0284]: type annotations needed.
- Show the fix: always annotate step results explicitly with `let result: Result<T, E> = ...`.
- Use the content from 09-RESEARCH.md DOCS-03 Entry 3.

**2. Contributing / Implementation Rules (DOCS-04)** — Insert a new `## Contributing` section before the `## License` section at the bottom. Content:
- A short paragraph: "This project follows 38 implementation rules documented in [project-context.md](_bmad-output/project-context.md). Read these before making changes."
- Link must be a relative path: `_bmad-output/project-context.md`.

Important: Insert "Troubleshooting" before "Container Deployment". Insert "Contributing" before "License". Do NOT change existing content.
  </action>
  <verify>
    <automated>cd /home/esa/git/durable-rust && grep -c "Troubleshooting" README.md && grep -c "Send.*static" README.md && grep -c "Serialize.*DeserializeOwned" README.md && grep -c "type annotations" README.md && grep -c "project-context.md" README.md</automated>
  </verify>
  <done>"Troubleshooting" section exists near bottom of README with all three FAQ entries showing actual compiler errors and fixes. "Contributing" section exists before License with link to project-context.md.</done>
</task>

</tasks>

<verification>
After both tasks complete:
- `cargo test --workspace` passes (no regressions from README changes)
- README.md contains all 5 new sections: Determinism Rules, Error Handling, Troubleshooting, Contributing, and parallel boxing comment
- New sections are in the correct order relative to existing sections
- All code examples in README are syntactically correct Rust
</verification>

<success_criteria>
- README has "Determinism Rules" with do/don't table and code examples after Operations Guide
- README has "Error Handling" with three-arm Result match pattern
- README has "Troubleshooting" FAQ with 3 compiler error entries
- README has "Contributing" section linking to project-context.md
- Parallel section has comment explaining boxing/type alias
- `cargo test --workspace` passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-documentation-overhaul/09-01-SUMMARY.md`
</output>
