---
phase: 09-documentation-overhaul
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/migration-guide.md
  - crates/durable-lambda-core/src/types.rs
  - CLAUDE.md
  - crates/durable-lambda-core/Cargo.toml
  - crates/durable-lambda-closure/Cargo.toml
  - crates/durable-lambda-trait/Cargo.toml
  - crates/durable-lambda-builder/Cargo.toml
  - crates/durable-lambda-macro/Cargo.toml
  - crates/durable-lambda-testing/Cargo.toml
autonomous: true
requirements:
  - DOCS-05
  - DOCS-06
  - DOCS-08
  - DOCS-09
  - DOCS-10

must_haves:
  truths:
    - "Migration guide has a 'Python Determinism Anti-Patterns in Rust' section with Python-to-Rust mapping table"
    - "BatchResult<T> rustdoc shows per-item status checking with BatchItemStatus::Succeeded and Failed arms"
    - "CallbackHandle rustdoc has ASCII diagram showing two separate operation IDs for create_callback and callback_result"
    - "CLAUDE.md documents DurableContextOps trait and new features from Phases 5-8"
    - "All 6 Cargo.toml files have description, keywords, and categories fields"
  artifacts:
    - path: "docs/migration-guide.md"
      provides: "Determinism anti-patterns section with Python-specific framing"
      contains: "Determinism Anti-Patterns"
    - path: "crates/durable-lambda-core/src/types.rs"
      provides: "Enhanced rustdoc for BatchResult and CallbackHandle"
      contains: "BatchItemStatus::Failed"
    - path: "CLAUDE.md"
      provides: "Updated architecture docs reflecting DurableContextOps and new features"
      contains: "DurableContextOps"
    - path: "crates/durable-lambda-core/Cargo.toml"
      provides: "Package metadata"
      contains: "keywords"
  key_links:
    - from: "CLAUDE.md"
      to: "crates/durable-lambda-core/src/ops_trait.rs"
      via: "documentation reference"
      pattern: "ops_trait"
---

<objective>
Update migration guide, inline rustdoc, CLAUDE.md, and all 6 Cargo.toml files with missing documentation content.

Purpose: Complete the remaining 5 DOCS requirements that don't touch README.md, covering determinism anti-patterns for Python migrants, per-item BatchResult checking examples, callback protocol visualization, CLAUDE.md accuracy, and crate metadata.

Output: Updated migration guide, types.rs rustdoc, CLAUDE.md, and all 6 Cargo.toml files.
</objective>

<execution_context>
@/home/esa/.claude/get-shit-done/workflows/execute-plan.md
@/home/esa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-documentation-overhaul/09-CONTEXT.md
@.planning/phases/09-documentation-overhaul/09-RESEARCH.md
@docs/migration-guide.md
@crates/durable-lambda-core/src/types.rs
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Determinism Anti-Patterns to Migration Guide</name>
  <files>docs/migration-guide.md</files>
  <action>
Edit docs/migration-guide.md to add a new subsection in the existing "Gotchas" section (which starts at line 389).

Per user decision: Add a "Python Determinism Anti-Patterns in Rust" subsection. The existing "Gotcha 1: Determinism" already has a SystemTime example. Add a new subsection after Gotcha 1 (or extend Gotcha 1) with Python-specific framing.

Content (from 09-RESEARCH.md DOCS-05):
- Title: `### Python Determinism Anti-Patterns in Rust`
- Intro: "Python durable execution silently serializes many non-deterministic values. Rust requires you to be explicit. These Python patterns cause replay failures in Rust:"
- Table with 4 rows:
  | Python Pattern | Why It Works in Python | Rust Equivalent (Correct) |
  - `datetime.now()` outside activity -> `ctx.step("ts", || async { Ok(Utc::now()) }).await?`
  - `uuid.uuid4()` outside activity -> `ctx.step("id", || async { Ok(Uuid::new_v4()) }).await?`
  - `random.random()` outside activity -> `ctx.step("rng", || async { Ok(rand::random::<f64>()) }).await?`
  - Branching on external env vars -> Read env vars in a `step()` if they affect workflow branching
- Closing rule: "If a value must be the same across all replays of a workflow execution, it must be produced inside a `ctx.step()` so it is checkpointed."

Insert after the existing Gotcha 1 determinism section (after line ~416 where the "Rule:" line ends) and before Gotcha 2.
  </action>
  <verify>
    <automated>cd /home/esa/git/durable-rust && grep -c "Python Determinism Anti-Patterns" docs/migration-guide.md && grep -c "datetime.now" docs/migration-guide.md</automated>
  </verify>
  <done>Migration guide "Gotchas" section has a "Python Determinism Anti-Patterns in Rust" subsection with Python-to-Rust mapping table covering datetime.now(), uuid.uuid4(), random.random(), and env vars.</done>
</task>

<task type="auto">
  <name>Task 2: Add BatchResult and CallbackHandle Rustdoc Examples</name>
  <files>crates/durable-lambda-core/src/types.rs</files>
  <action>
Edit crates/durable-lambda-core/src/types.rs to enhance two rustdoc blocks:

**1. BatchResult<T> per-item status checking (DOCS-06):**
Find the `pub struct BatchResult<T>` definition (line ~692). Expand the existing rustdoc example above it to include a per-item status checking example. Add after the existing example (which shows struct construction) a new example block showing:
- Iterating over `result.results` and matching on `item.status`
- `BatchItemStatus::Succeeded` -> access `item.result`
- `BatchItemStatus::Failed` -> access `item.error`
- Collecting only successful values with `filter_map`
- Use `# ignore` or `no_run` if the example requires types not available in doctest scope. Prefer making it compile if possible by constructing the types inline.

Source content from 09-RESEARCH.md DOCS-06 section. Adapt the code to be a valid rustdoc example (with `///` comment prefix, proper imports).

**2. CallbackHandle two-phase operation ID diagram (DOCS-09):**
Find `pub struct CallbackHandle` (line ~557). Add to its existing rustdoc (which already has a usage example) an ASCII diagram showing the two-phase callback protocol:
- Invocation 1: `create_callback` gets op_id blake2b("1") -> START + SUCCEED. `callback_result` gets op_id blake2b("2") -> START -> SUSPEND.
- External system sends callback signal.
- Invocation 2: Both operations replay from cache, workflow continues.
- Use `///` doc comment format, NOT a code block (it's a diagram, not runnable code).

Source content from 09-RESEARCH.md DOCS-09 section.

Important: Both additions must be valid rustdoc. After editing, run `cargo test --doc -p durable-lambda-core` to verify doctest compilation. If any doctest fails, fix the example (likely needs `# ignore` or `no_run` attribute, or proper import adjustments).
  </action>
  <verify>
    <automated>cd /home/esa/git/durable-rust && cargo test --doc -p durable-lambda-core 2>&1 | tail -5</automated>
  </verify>
  <done>BatchResult rustdoc includes per-item status checking example showing Succeeded/Failed matching and filter_map collection. CallbackHandle rustdoc includes ASCII diagram showing two-phase operation ID protocol. `cargo test --doc -p durable-lambda-core` passes.</done>
</task>

<task type="auto">
  <name>Task 3: Update CLAUDE.md and Add Cargo.toml Metadata</name>
  <files>CLAUDE.md, crates/durable-lambda-core/Cargo.toml, crates/durable-lambda-closure/Cargo.toml, crates/durable-lambda-trait/Cargo.toml, crates/durable-lambda-builder/Cargo.toml, crates/durable-lambda-macro/Cargo.toml, crates/durable-lambda-testing/Cargo.toml</files>
  <action>
**1. CLAUDE.md updates (DOCS-08):**
Edit CLAUDE.md to reflect current architecture after Phases 1-8:

a) In the "Key Internals" section (starts at line ~49), add a new bullet point for the DurableContextOps trait:
```
- **`DurableContextOps` trait** (`core/src/ops_trait.rs`): Single trait defining all context methods. Implemented by `DurableContext`. Wrapper contexts (`ClosureContext`, `TraitContext`, `BuilderContext`) delegate to `DurableContext`. To add or change a context method, edit `ops_trait.rs` + `context.rs` only.
```

b) In the "Critical Rules" section, add a new subsection `### New Features (Phases 5-8)` with these bullet points:
- **Step timeout**: `StepOptions::new().timeout_seconds(u64)` — wraps closure in tokio::time::timeout
- **Conditional retry**: `StepOptions::new().retry_if(|e: &E| ...)` — predicate checked before consuming retry budget
- **Batch checkpoint**: `ctx.enable_batch_mode()` — multiple sequential steps share a single checkpoint call
- **Saga / compensation**: `ctx.step_with_compensation("name", forward_fn, compensate_fn)` — registers durable rollback
- **Proc-macro validation**: `#[durable_execution]` validates second param is `DurableContext` and return is `Result<_, DurableError>` at compile time
- **Builder configuration**: `.with_tracing(subscriber)` and `.with_error_handler(fn)` on `DurableHandlerBuilder`

Use the exact content from 09-RESEARCH.md DOCS-08 section. Keep CLAUDE.md's existing terse bullet-point style.

**2. Cargo.toml metadata for all 6 crates (DOCS-10):**
Add `description`, `keywords`, and `categories` fields to the `[package]` section of each crate's Cargo.toml, after the `edition` line.

Per-crate descriptions (from 09-RESEARCH.md DOCS-10):
- `durable-lambda-core`: "Core replay engine, types, and operation logic for AWS Lambda durable execution in Rust"
- `durable-lambda-closure`: "Closure-native API style for AWS Lambda durable execution workflows"
- `durable-lambda-trait`: "Trait-based API style for AWS Lambda durable execution workflows"
- `durable-lambda-builder`: "Builder-pattern API style for AWS Lambda durable execution workflows"
- `durable-lambda-macro`: "Proc-macro for zero-boilerplate AWS Lambda durable execution handler registration"
- `durable-lambda-testing`: "MockDurableContext and assertion helpers for testing durable Lambda handlers without AWS credentials"

Shared keywords (same for all 6):
```toml
keywords = ["aws", "lambda", "durable-execution", "serverless", "workflow"]
```

Shared categories (same for all 6):
```toml
categories = ["api-bindings", "asynchronous"]
```

Per user discretion decision: omit `license` and `repository` fields (internal project).

After editing, run `cargo build --workspace` to verify Cargo.toml changes are valid.
  </action>
  <verify>
    <automated>cd /home/esa/git/durable-rust && grep -c "DurableContextOps" CLAUDE.md && grep -c "Step timeout" CLAUDE.md && cargo metadata --format-version 1 --no-deps 2>&1 | python3 -c "import sys,json; d=json.load(sys.stdin); missing=[p['name'] for p in d['packages'] if not p.get('description')]; print(f'{len(missing)} crates missing description') if missing else print('all crates have description')"</automated>
  </verify>
  <done>CLAUDE.md "Key Internals" includes DurableContextOps trait entry. CLAUDE.md "Critical Rules" includes "New Features (Phases 5-8)" subsection. All 6 Cargo.toml files have description, keywords, and categories. `cargo build --workspace` passes.</done>
</task>

</tasks>

<verification>
After all tasks complete:
- `cargo test --workspace` passes (no regressions, doctests compile)
- `cargo build --workspace` passes (Cargo.toml metadata valid)
- Migration guide has Python determinism anti-patterns section
- types.rs BatchResult has per-item status checking example
- types.rs CallbackHandle has two-phase operation ID diagram
- CLAUDE.md reflects DurableContextOps trait and Phases 5-8 features
- All 6 Cargo.toml files have description, keywords, categories
</verification>

<success_criteria>
- Migration guide "Gotchas" section has Python-specific determinism anti-patterns table
- BatchResult rustdoc has compilable per-item status checking example
- CallbackHandle rustdoc has two-phase callback protocol ASCII diagram
- CLAUDE.md documents DurableContextOps and all 6 new features from Phases 5-8
- All 6 crate Cargo.toml files have description, keywords, and categories metadata
- `cargo test --workspace` and `cargo build --workspace` both pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-documentation-overhaul/09-02-SUMMARY.md`
</output>
