---
phase: 18-crate-metadata
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - LICENSE-MIT
  - LICENSE-APACHE
  - crates/durable-lambda-core/Cargo.toml
  - crates/durable-lambda-macro/Cargo.toml
  - crates/durable-lambda-closure/Cargo.toml
  - crates/durable-lambda-trait/Cargo.toml
  - crates/durable-lambda-builder/Cargo.toml
  - crates/durable-lambda-testing/Cargo.toml
  - examples/closure-style/Cargo.toml
  - examples/macro-style/Cargo.toml
  - examples/trait-style/Cargo.toml
  - examples/builder-style/Cargo.toml
autonomous: true
requirements:
  - META-01
  - META-02

must_haves:
  truths:
    - "Every publishable crate Cargo.toml contains license, repository, homepage, readme, documentation, description, categories, and keywords"
    - "All 6 crates share version 0.1.0 managed via workspace-level version inheritance"
    - "Non-publishable crates (examples, tests, compliance) have publish = false"
    - "cargo metadata --no-deps shows no missing required fields for any publishable crate"
  artifacts:
    - path: "Cargo.toml"
      provides: "[workspace.package] section with inherited version, edition, license, repository, homepage, keywords, categories"
      contains: "[workspace.package]"
    - path: "LICENSE-MIT"
      provides: "MIT license text with correct copyright"
      min_lines: 15
    - path: "LICENSE-APACHE"
      provides: "Apache 2.0 license text"
      min_lines: 150
  key_links:
    - from: "crates/*/Cargo.toml"
      to: "Cargo.toml"
      via: "workspace inheritance (version.workspace = true, license.workspace = true, etc.)"
      pattern: "workspace = true"
---

<objective>
Add complete crates.io metadata to all 6 publishable crates using workspace-level version inheritance, create dual license files, and mark non-publishable crates as `publish = false`.

Purpose: Satisfy crates.io's required fields so `cargo publish --dry-run` passes for metadata (a prerequisite for Phase 19's publishing infrastructure).
Output: Updated Cargo.toml files with full metadata, LICENSE-MIT and LICENSE-APACHE at repo root.
</objective>

<execution_context>
@/home/esa/.claude/get-shit-done/workflows/execute-plan.md
@/home/esa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-crate-metadata/18-CONTEXT.md
@Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create license files and configure workspace-level package metadata</name>
  <files>
    Cargo.toml,
    LICENSE-MIT,
    LICENSE-APACHE,
    crates/durable-lambda-core/Cargo.toml,
    crates/durable-lambda-macro/Cargo.toml,
    crates/durable-lambda-closure/Cargo.toml,
    crates/durable-lambda-trait/Cargo.toml,
    crates/durable-lambda-builder/Cargo.toml,
    crates/durable-lambda-testing/Cargo.toml,
    examples/closure-style/Cargo.toml,
    examples/macro-style/Cargo.toml,
    examples/trait-style/Cargo.toml,
    examples/builder-style/Cargo.toml
  </files>
  <action>
    **1. Create LICENSE-MIT** at repo root with standard MIT license text.
    - Copyright holder: "The durable-rust Contributors"
    - Year: 2026

    **2. Create LICENSE-APACHE** at repo root with full Apache License 2.0 text.
    - Standard unmodified Apache 2.0 from https://www.apache.org/licenses/LICENSE-2.0

    **3. Add `[workspace.package]` section to root Cargo.toml** (place it after `[workspace]` block, before `[workspace.dependencies]`):
    ```toml
    [workspace.package]
    version = "0.1.0"
    edition = "2021"
    license = "MIT OR Apache-2.0"
    repository = "https://github.com/pgdad/durable-rust"
    homepage = "https://github.com/pgdad/durable-rust"
    keywords = ["aws", "lambda", "durable-execution", "serverless", "workflow"]
    categories = ["api-bindings", "asynchronous"]
    ```

    **4. Update each of the 6 publishable crate Cargo.toml files.** For each crate:
    - Replace `version = "0.1.0"` with `version.workspace = true`
    - Replace `edition = "2021"` with `edition.workspace = true`
    - Remove `keywords = [...]` line (now inherited)
    - Remove `categories = [...]` line (now inherited)
    - Add these lines in the `[package]` section:
      ```toml
      license.workspace = true
      repository.workspace = true
      homepage.workspace = true
      keywords.workspace = true
      categories.workspace = true
      readme = "README.md"
      documentation = "https://docs.rs/{crate-name}"
      ```
    - Keep `name` and `description` as-is (per-crate values, NOT inherited)

    **5. Add `publish = false` to the 4 example crate Cargo.toml files** that don't have it yet:
    - examples/closure-style/Cargo.toml
    - examples/macro-style/Cargo.toml
    - examples/trait-style/Cargo.toml
    - examples/builder-style/Cargo.toml
    (tests/e2e, tests/parity, and compliance/rust already have `publish = false`)

    **6. Verify the workspace builds:** Run `cargo build --workspace` to confirm no Cargo.toml syntax errors.
  </action>
  <verify>
    <automated>cd /home/esa/git/durable-rust && cargo build --workspace 2>&1 | tail -5</automated>
  </verify>
  <done>
    - LICENSE-MIT and LICENSE-APACHE exist at repo root
    - Root Cargo.toml has [workspace.package] with version, edition, license, repository, homepage, keywords, categories
    - All 6 publishable crates inherit version, edition, license, repository, homepage, keywords, categories from workspace
    - All 6 publishable crates have readme and documentation fields
    - All 4 example crates have publish = false
    - cargo build --workspace succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Validate crate metadata completeness with cargo metadata</name>
  <files>
    (no files modified — validation only)
  </files>
  <action>
    **1. Run `cargo metadata --no-deps --format-version 1` and parse output** to verify each of the 6 publishable crates has all required fields populated:
    - license: "MIT OR Apache-2.0"
    - repository: "https://github.com/pgdad/durable-rust"
    - homepage: "https://github.com/pgdad/durable-rust"
    - documentation: "https://docs.rs/{crate-name}"
    - readme: "README.md"
    - description: (non-empty string)
    - keywords: 5 keywords
    - categories: 2 categories

    **2. Run `cargo publish --dry-run -p {crate}` for durable-lambda-core** as a representative check that metadata passes crates.io validation. This may warn about missing README.md files (expected — Plan 02 creates those), but should NOT fail on missing Cargo.toml fields.

    **3. Verify non-publishable crates** by checking that `cargo metadata` shows `publish: []` (empty list = cannot publish) for example/test/compliance crates.

    **4. If any metadata field is missing or incorrect,** fix the offending Cargo.toml file and re-run validation.
  </action>
  <verify>
    <automated>cd /home/esa/git/durable-rust && cargo metadata --no-deps --format-version 1 | python3 -c "
import json, sys
data = json.load(sys.stdin)
PUBLISHABLE = ['durable-lambda-core', 'durable-lambda-macro', 'durable-lambda-closure', 'durable-lambda-trait', 'durable-lambda-builder', 'durable-lambda-testing']
REQUIRED = ['license', 'repository', 'homepage', 'documentation', 'description', 'readme']
errors = []
for pkg in data['packages']:
    if pkg['name'] in PUBLISHABLE:
        for field in REQUIRED:
            val = pkg.get(field)
            if not val:
                errors.append(f'{pkg[\"name\"]}: missing {field}')
        if len(pkg.get('keywords', [])) == 0:
            errors.append(f'{pkg[\"name\"]}: no keywords')
        if len(pkg.get('categories', [])) == 0:
            errors.append(f'{pkg[\"name\"]}: no categories')
if errors:
    print('FAIL'); [print(f'  - {e}') for e in errors]; sys.exit(1)
else:
    print(f'PASS: all {len(PUBLISHABLE)} crates have complete metadata')
"</automated>
  </verify>
  <done>
    - All 6 publishable crates have every required metadata field populated
    - cargo metadata shows no missing fields
    - Non-publishable crates cannot be accidentally published
  </done>
</task>

</tasks>

<verification>
1. `ls LICENSE-MIT LICENSE-APACHE` — both files exist
2. `grep -c "workspace.package" Cargo.toml` — returns 1
3. `cargo metadata --no-deps` validation script passes for all 6 crates
4. `cargo build --workspace` succeeds
5. All example/test/compliance crates have `publish = false`
</verification>

<success_criteria>
- LICENSE-MIT and LICENSE-APACHE exist at repo root with proper content
- [workspace.package] in root Cargo.toml manages version, edition, license, repository, homepage, keywords, categories
- All 6 publishable crates inherit workspace fields and have readme + documentation
- All non-publishable crates marked with publish = false
- cargo metadata --no-deps shows no missing fields for any publishable crate
</success_criteria>

<output>
After completion, create `.planning/phases/18-crate-metadata/18-01-SUMMARY.md`
</output>
