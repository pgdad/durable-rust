---
phase: 16-advanced-feature-tests
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - scripts/test-all.sh
autonomous: false
requirements:
  - ADV-01
  - ADV-02
  - ADV-03
  - ADV-04
user_setup:
  - service: aws-adfs
    why: "ADFS credentials required for terraform apply and Lambda invocation"
    env_vars:
      - name: AWS_PROFILE
        source: "adfs profile — run adfs-auth if expired"
    dashboard_config: []

must_haves:
  truths:
    - "4 new Lambda functions exist in AWS with durable_config and live alias"
    - "4 new container images are pushed to ECR (48 total)"
    - "Saga test returns compensation_sequence in LIFO order (charge_card, book_flight, book_hotel)"
    - "Step timeout test returns FunctionError with timeout indication"
    - "Conditional retry test with non-retryable error returns FunctionError immediately"
    - "Batch checkpoint test returns steps_completed=5 and batch_mode=true"
  artifacts:
    - path: "scripts/test-all.sh"
      provides: "Integration test results for 4 advanced features"
      contains: "test_closure_saga_compensation"
  key_links:
    - from: "scripts/test-all.sh"
      to: "AWS Lambda"
      via: "invoke_sync calls real Lambda functions"
      pattern: "invoke_sync.*closure-saga-compensation"
    - from: "infra/lambda.tf"
      to: "AWS Lambda"
      via: "terraform apply creates 4 new functions"
      pattern: "closure-saga-compensation"
---

<objective>
Deploy the 4 new Lambda functions to AWS, build and push their container images, and run the integration tests to validate saga/compensation, step timeout, conditional retry, and batch checkpoint against real AWS durable execution.

Purpose: Proves the 4 advanced SDK features work correctly against live AWS infrastructure, not just unit tests.

Output: 4 green integration tests, confirmed by `scripts/test-all.sh` output.
</objective>

<execution_context>
@/home/esa/.claude/get-shit-done/workflows/execute-plan.md
@/home/esa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-advanced-feature-tests/16-RESEARCH.md
@.planning/phases/16-advanced-feature-tests/16-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy infrastructure and build images</name>
  <files>scripts/test-all.sh</files>
  <action>
**Step 1: Verify ADFS credentials are valid.**
```bash
aws sts get-caller-identity --profile adfs --region us-east-2
```
If expired, this task cannot proceed. Create a checkpoint for the user to refresh credentials.

**Step 2: Run terraform apply to create the 4 new Lambda functions.**
```bash
cd /home/esa/git/durable-rust/infra
terraform apply -parallelism=5 -auto-approve
```
CRITICAL: Use `-parallelism=5` to avoid ResourceConflictException at 48-function scale (per STATE.md decision).

Verify: `terraform plan` shows no pending changes. The 4 new functions and aliases should now exist.

**Step 3: Build and push all container images (including 4 new ones).**
```bash
bash /home/esa/git/durable-rust/scripts/build-images.sh
```
This rebuilds and pushes all 48 images (Docker layer cache will make the 44 existing images fast; only the 4 new ones need fresh builds). The script gates on verify-prerequisites.sh and verifies ECR image count = 48 at the end.

**Step 4: Update Lambda functions to use new images.**
After pushing new images, the Lambda functions need their image URIs refreshed. Run terraform apply again to update the image_uri references and publish new versions:
```bash
cd /home/esa/git/durable-rust/infra
terraform apply -parallelism=5 -auto-approve
```

Verify: `terraform plan` shows no pending changes.

If build-images.sh fails, check:
- Docker daemon running (`docker info`)
- ADFS credentials not expired
- ECR login succeeded
- IMAGE_COUNT check is 48 (not 44 — this was updated in Plan 01)
  </action>
  <verify>
    <automated>cd /home/esa/git/durable-rust/infra && terraform plan -detailed-exitcode 2>&1 | tail -5</automated>
  </verify>
  <done>Terraform shows no pending changes (exit code 0). ECR has 48 images. 4 new Lambda functions (dr-closure-saga-compensation-*, dr-closure-step-timeout-*, dr-closure-conditional-retry-*, dr-closure-batch-checkpoint-*) exist with live aliases.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify integration tests pass against live Lambda</name>
  <action>
Deployed 4 new advanced-feature Lambda functions (saga, timeout, retry, batch) and pushed 48 container images to ECR. Ready to run integration tests.

Run the 4 Phase 16 integration tests:
```bash
bash scripts/test-all.sh closure-saga-compensation
bash scripts/test-all.sh closure-step-timeout
bash scripts/test-all.sh closure-conditional-retry
bash scripts/test-all.sh closure-batch-checkpoint
```

Expected results:
- **closure-saga-compensation**: PASS — returns `{"status":"rolled_back","compensation_sequence":["charge_card","book_flight","book_hotel"],"all_succeeded":true}`
- **closure-step-timeout**: PASS — returns FunctionError with timeout indication (step sleeps 60s, timeout set to 2s)
- **closure-conditional-retry**: PASS — non-retryable error path returns FunctionError immediately (retry_if predicate returns false)
- **closure-batch-checkpoint**: PASS — returns `{"steps_completed":5,"batch_mode":true}` with status 200

If any test fails, share the error output for diagnosis.

NOTE: Each test invokes a real Lambda function. ADFS credentials must be valid. If you see CREDENTIAL_EXPIRED, refresh ADFS auth first.
  </action>
  <verify>
    <automated>bash /home/esa/git/durable-rust/scripts/test-all.sh closure-saga-compensation 2>&1 | tail -3</automated>
  </verify>
  <done>All 4 advanced-feature tests (saga, timeout, retry, batch) show PASS in test-all.sh output. User has confirmed results.</done>
</task>

</tasks>

<verification>
1. `terraform plan` shows no pending changes (all 48 functions deployed)
2. `bash scripts/test-all.sh closure-saga-compensation` — PASS
3. `bash scripts/test-all.sh closure-step-timeout` — PASS
4. `bash scripts/test-all.sh closure-conditional-retry` — PASS
5. `bash scripts/test-all.sh closure-batch-checkpoint` — PASS
</verification>

<success_criteria>
- All 4 advanced-feature Lambda functions are deployed to AWS and invocable
- Saga test confirms LIFO compensation order: charge_card, book_flight, book_hotel
- Step timeout test confirms FunctionError is returned when step exceeds timeout threshold
- Conditional retry test confirms retry_if predicate correctly prevents retry on non-matching errors
- Batch checkpoint test confirms handler succeeds with 5 steps in batch mode
- All 4 test cases show PASS in test-all.sh output
</success_criteria>

<output>
After completion, create `.planning/phases/16-advanced-feature-tests/16-02-SUMMARY.md`
</output>
