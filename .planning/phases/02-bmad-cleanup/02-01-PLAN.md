---
phase: 02-bmad-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - _bmad-output/ (removed)
  - _bmad/ (removed)
  - .claude/skills/bmad-* (54 dirs removed)
  - .planning/research/STACK.md
  - .planning/research/ARCHITECTURE.md
  - .planning/research/PITFALLS.md
  - .planning/research/SUMMARY.md
  - .planning/research/FEATURES.md
  - .planning/PROJECT.md
  - .planning/MILESTONES.md
  - .planning/phases/01-gsd-infrastructure/01-RESEARCH.md
  - .planning/phases/01-gsd-infrastructure/01-01-PLAN.md
  - .planning/phases/01-gsd-infrastructure/01-01-SUMMARY.md
autonomous: true
requirements: [BMAD-01, BMAD-02, BMAD-03]

must_haves:
  truths:
    - "_bmad-output/ directory does not exist in the repository"
    - "_bmad/ directory does not exist in the repository"
    - "No bmad-* directories exist under .claude/skills/"
    - "Zero functional _bmad references remain in tracked files"
    - "crates/, tests/, examples/, docs/ are completely untouched"
  artifacts:
    - path: "_bmad-output/"
      provides: "Must NOT exist after plan completion"
    - path: "_bmad/"
      provides: "Must NOT exist after plan completion"
  key_links:
    - from: ".planning/ files"
      to: "_bmad paths"
      via: "text references"
      pattern: "Zero matches for _bmad in tracked files excluding .git/"
---

<objective>
Remove all BMAD artifacts from the repository in four dedicated atomic commits, then verify zero orphaned references remain and Rust source is untouched.

Purpose: Complete the GSD tooling transition by eliminating all BMAD framework presence from the working tree.
Output: Clean repository with no _bmad directories and no _bmad references in tracked files. Four atomic commits in git history.
</objective>

<execution_context>
@/Users/esa/.claude/get-shit-done/workflows/execute-plan.md
@/Users/esa/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-bmad-cleanup/02-RESEARCH.md
@.planning/phases/02-bmad-cleanup/02-CONTEXT.md
@.planning/phases/02-bmad-cleanup/02-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove _bmad-output/ and _bmad/ directories</name>
  <files>_bmad-output/ (removed), _bmad/ (removed)</files>
  <action>
Two separate commits, in strict order per user decision.

**Commit 1 — Remove _bmad-output/ (BMAD-01):**
1. Verify the directory exists: `ls _bmad-output/`
2. Run `git rm -r _bmad-output/`
3. Commit: `chore: remove _bmad-output/ planning artifacts`
4. Verify: `test ! -d _bmad-output && echo "REMOVED"` must print REMOVED
5. Verify Rust source untouched: `git diff HEAD~1 -- crates/ tests/ examples/ docs/` must be empty

**Commit 2 — Remove _bmad/ (BMAD-02):**
1. Verify the directory exists: `ls _bmad/`
2. Run `git rm -r _bmad/`
3. Commit: `chore: remove _bmad/ framework tooling`
4. Verify: `test ! -d _bmad && echo "REMOVED"` must print REMOVED
5. Verify Rust source untouched: `git diff HEAD~1 -- crates/ tests/ examples/ docs/` must be empty

CRITICAL constraints:
- Use exact paths only, never globs
- Each removal in its own dedicated commit
- _bmad-output/ MUST be removed before _bmad/
  </action>
  <verify>
    <automated>test ! -d _bmad-output && test ! -d _bmad && echo "PASS"</automated>
  </verify>
  <done>_bmad-output/ and _bmad/ no longer exist in repository. Two atomic commits in git history. Rust source directories confirmed untouched after each commit.</done>
</task>

<task type="auto">
  <name>Task 2: Remove .claude/skills/bmad-* directories</name>
  <files>.claude/skills/bmad-* (54 directories removed)</files>
  <action>
One dedicated commit removing all 54 bmad-prefixed skill directories.

**Steps:**
1. List current bmad skill directories to confirm targets: `ls .claude/skills/ | grep bmad`
2. For each directory returned, run `git rm -r .claude/skills/{dirname}` using the EXACT directory name — do NOT use shell glob expansion on the git rm command itself. You may use a for-loop that iterates over the resolved list:
   ```bash
   for dir in $(ls .claude/skills/ | grep '^bmad-'); do
     git rm -r ".claude/skills/$dir"
   done
   ```
   This resolves exact paths first, then passes each individually to git rm.
3. Commit: `chore: remove .claude/skills bmad skill directories`
4. Verify: `find .claude/skills -name 'bmad-*' -maxdepth 1 2>/dev/null | wc -l` must return 0
5. Note: `.claude/skills/` directory will be empty or removed by git after this — that is expected. GSD skills live at `~/.claude/get-shit-done/`.
6. Verify Rust source untouched: `git diff HEAD~1 -- crates/ tests/ examples/ docs/` must be empty

CRITICAL: Do not use `git rm -r .claude/skills/bmad-*` directly as a shell glob. Resolve the list first, then remove each by exact path.
  </action>
  <verify>
    <automated>test -z "$(find .claude/skills -name 'bmad-*' -maxdepth 1 2>/dev/null)" && echo "PASS"</automated>
  </verify>
  <done>All 54 bmad-prefixed skill directories removed from .claude/skills/ in one atomic commit. Rust source confirmed untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Clean _bmad references from .planning/ files and final verification</name>
  <files>
    .planning/research/STACK.md
    .planning/research/ARCHITECTURE.md
    .planning/research/PITFALLS.md
    .planning/research/SUMMARY.md
    .planning/research/FEATURES.md
    .planning/PROJECT.md
    .planning/MILESTONES.md
    .planning/phases/01-gsd-infrastructure/01-RESEARCH.md
    .planning/phases/01-gsd-infrastructure/01-01-PLAN.md
    .planning/phases/01-gsd-infrastructure/01-01-SUMMARY.md
  </files>
  <action>
One dedicated commit cleaning residual _bmad text references, then full verification.

**Files to edit (remove or rephrase _bmad path mentions):**
1. `.planning/research/STACK.md` — remove/rephrase _bmad references
2. `.planning/research/ARCHITECTURE.md` — remove/rephrase _bmad references
3. `.planning/research/PITFALLS.md` — remove/rephrase _bmad references
4. `.planning/research/SUMMARY.md` — remove/rephrase _bmad references
5. `.planning/research/FEATURES.md` — remove/rephrase _bmad references
6. `.planning/PROJECT.md` — remove/rephrase _bmad references
7. `.planning/MILESTONES.md` — remove/rephrase _bmad references
8. `.planning/phases/01-gsd-infrastructure/01-RESEARCH.md` — remove/rephrase _bmad references
9. `.planning/phases/01-gsd-infrastructure/01-01-PLAN.md` — remove/rephrase _bmad references
10. `.planning/phases/01-gsd-infrastructure/01-01-SUMMARY.md` — remove/rephrase _bmad references

**Files to NOT edit (they are definitional or historical records):**
- `.planning/REQUIREMENTS.md` — contains BMAD-01/02/03 requirement definitions; only update checkboxes to `[x]` after verification passes, do NOT delete requirement text
- `.planning/ROADMAP.md` — phase description references are definitional
- `.planning/STATE.md` — blocker/decision references are historical context
- `.planning/phases/02-bmad-cleanup/02-CONTEXT.md` — historical planning record
- `.planning/phases/02-bmad-cleanup/02-RESEARCH.md` — historical research record

**Editing guidelines (per user decision — Claude's discretion on exact wording):**
- Don't rewrite entire documents; only update specific lines referencing _bmad paths
- Brief historical notes like "previously managed with BMAD" are acceptable
- Remove path references that point to now-deleted directories
- Keep document structure and meaning intact

**After edits:**
1. Stage all changed files: `git add` each modified file by name
2. Commit: `docs: clean _bmad references from .planning/ files`

**Final verification (BMAD-03 complete):**
3. Run: `grep -r "_bmad" . --include="*.md" --include="*.yaml" --include="*.json" --include="*.toml" --include="*.rs" --exclude-dir=.git`
   - This WILL return hits from 02-CONTEXT.md, 02-RESEARCH.md, 02-VALIDATION.md, REQUIREMENTS.md, ROADMAP.md, STATE.md — these are definitional/historical and are expected
   - No OTHER files should match. Any unexpected match is a missed reference — fix it
4. Run: `git diff HEAD~4 -- crates/ tests/ examples/ docs/` — must return empty (Rust source untouched across all 4 commits)
5. Update `.planning/REQUIREMENTS.md`: change BMAD-01, BMAD-02, BMAD-03 checkboxes from `[ ]` to `[x]`
6. Stage and amend the doc cleanup commit to include the REQUIREMENTS.md checkbox update, OR create a tiny follow-up commit

**Note on BMAD-03 scope:** Per user decision, "no orphaned references" means functional references only — paths that would cause errors. Planning doc references that describe Phase 2 work (REQUIREMENTS.md requirement text, ROADMAP.md phase description, STATE.md blockers) are definitional, not orphaned.
  </action>
  <verify>
    <automated>! grep -r "_bmad" .planning/research/ --include="*.md" 2>/dev/null && ! grep -r "_bmad" .planning/PROJECT.md 2>/dev/null && ! grep -r "_bmad" .planning/MILESTONES.md 2>/dev/null && ! grep -r "_bmad" .planning/phases/01-gsd-infrastructure/ --include="*.md" 2>/dev/null && echo "PASS"</automated>
  </verify>
  <done>All non-definitional _bmad references removed from .planning/ files. REQUIREMENTS.md checkboxes updated to [x]. grep confirms zero functional orphaned references. git diff confirms Rust source completely untouched across all 4 commits.</done>
</task>

</tasks>

<verification>
After all three tasks complete, run the full verification suite:

1. Directory removal: `test ! -d _bmad-output && test ! -d _bmad && echo "DIRS REMOVED"`
2. Skills removal: `test -z "$(find .claude/skills -name 'bmad-*' -maxdepth 1 2>/dev/null)" && echo "SKILLS REMOVED"`
3. Reference cleanup: `grep -r "_bmad" . --include="*.md" --include="*.yaml" --include="*.json" --include="*.toml" --include="*.rs" --exclude-dir=.git` — only definitional references in phase 02 files, REQUIREMENTS.md, ROADMAP.md, STATE.md
4. Rust source safety: `git diff HEAD~4 -- crates/ tests/ examples/ docs/` — must be empty
5. Commit count: `git log --oneline HEAD~4..HEAD` — should show exactly 4 commits
</verification>

<success_criteria>
- _bmad-output/ directory does not exist
- _bmad/ directory does not exist
- No bmad-* directories under .claude/skills/
- Zero functional _bmad references in tracked files (definitional references in phase planning docs are acceptable)
- crates/, tests/, examples/, docs/ have zero changes across all commits
- Exactly 4 atomic commits in git history for this phase
- REQUIREMENTS.md shows BMAD-01, BMAD-02, BMAD-03 as [x] complete
</success_criteria>

<output>
After completion, create `.planning/phases/02-bmad-cleanup/02-01-SUMMARY.md`
</output>
